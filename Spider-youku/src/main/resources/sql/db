CREATE DATABASE `spider_youkuvideo`;

CREATE TABLE `spider_youkuvideo`.`youku_video` (
  `vid` VARCHAR(50) NOT NULL,
  `title` VARCHAR(50) NULL,
  `url` VARCHAR(50) NULL,
  `category` VARCHAR(45) NULL,
  `categoryUrl` VARCHAR(45) NULL,
  PRIMARY KEY (`vid`))
COMMENT = 'youku video storage';
